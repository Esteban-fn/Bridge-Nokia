/* TIPO DE BRIDGE */
function tipoDeBridge() {
    let select = document.getElementById("tipoBridge");
    let valorSelecionado = select.value;
    console.log("Tipo selecionado:", valorSelecionado);
}